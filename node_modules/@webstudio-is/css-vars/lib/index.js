// src/index.ts
var CSS_VARIABLE_POSTFIX = "-w7ovd";
var cssVarsSet = /* @__PURE__ */ new Set();
var define = (name) => {
  if (cssVarsSet.has(name) === false) {
    cssVarsSet.add(name);
  }
  return `--${name}${CSS_VARIABLE_POSTFIX}`;
};
var use = (...args) => `var(${args.join(", ")})`;
var cssVars = { define, use };
export {
  cssVars
};
