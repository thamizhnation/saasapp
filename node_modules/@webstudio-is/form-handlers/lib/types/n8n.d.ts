import { type FormInfo, type Result } from "./shared";
export declare const n8nHandler: ({ formInfo, hookUrl, }: {
    formInfo: FormInfo;
    /** May containt basic authentication credentials,
     * e.g. https://user:pass@...app.n8n.cloud/webhook/... */
    hookUrl: string;
}) => Promise<Result>;
